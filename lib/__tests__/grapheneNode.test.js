const assert = require('assert');
const grapheneNode = require('../index.js');

describe('grapheneNode', function () {
  it('has a test', function () {
    assert(false, 'grapheneNode should have a test');
  });
});
